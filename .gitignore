# ── Python runtime ────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# ── Virtual environment (456 MB — recreate with: python3 -m venv venv) ────────
venv/
env/
.venv/

# ── Raw data & binary stores (too large / regeneratable) ──────────────────────
# Raw MERRA-2 NetCDF4 files + earthdata zip (298 MB) — symlink to ~/Project/dataset
dataset
dataset/
# v1 .npy flat grids (13 MB) — symlink to ~/Project/grids
grids
grids/
# TileDB binary array (11 MB, regenerated by: python3 scripts/build_tiledb.py)
tiledb_store/

# ── Build archives (day snapshots, not source) ─────────────────────────────────
*.zip
*.tar.gz
*.tar.bz2

# ── Runtime state ─────────────────────────────────────────────────────────────
logs/
run/
*.pid
*.log
nohup.out

# ── Personal scratch scripts (not part of the system) ─────────────────────────
varun_fix.sh
varun_fix_02.sh
varun_fix_03.sh
varun_fix_04.sh
PROGRESS_DAY1.md
PROGRESS_DAY2.md
APPLY_GUIDE.md
APPLY_GUIDE_DAY2.md
APPLY_GUIDE_DAY3.md
day_01_test_config_files/

# ── Editor / IDE / OS ─────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Claude Code session files ─────────────────────────────────────────────────
.claude/

# ── Misc ──────────────────────────────────────────────────────────────────────
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
